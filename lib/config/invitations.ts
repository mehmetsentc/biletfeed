/** WhatsApp / e-posta doğrudan paylaşımda en fazla PDF sayısı */
export const MAX_DIRECT_INVITATION_PDFS = 2;

/** Tek seferde oluşturulabilecek maksimum davetiye sayısı */
export const MAX_INVITATION_QUANTITY = 200;
